        console.log(err)
        bot.channels.cache.get(chnl.errors).send(`${err.stack}`)
        const embed = new MessageEmbed()
        .setTitle("Aw snap, an internal error!")
        .setThumbnail(resource.aw_snap)
        .setDescription("Something went wrong with retrieving the info! This issue will be sent to the developer, so please hold on :)")
        .setColor(colors.error)

        message.channel.send({embed}).then(m => {
            m.delete({ timeout: 10000 })
        })